# Used for data analysis it built on top of numpy
# Used fordata manipulation, visulyzation building ML models

#There are two main data structures avalable in pandas
  #1. Series- a column in a table(It is like 1D array or numpy array to keep same elements(Homozeniuos elements))
  #2. DataFrame - a table

pd.series
pd.datafrmae
